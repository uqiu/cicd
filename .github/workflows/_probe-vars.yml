name: Probe vars in with
on:
  workflow_dispatch:
jobs:
  call:
    permissions:
      contents: read
      packages: write
    uses: ./.github/workflows/_probe-leaf.yml
    with:
      s: ${{ vars.PROBE_ABSENT || 'fallback' }}
